import guilded_webhook from "./index.js";

const Webhook = guilded_webhook.Webhook;
const MessageBuilder = guilded_webhook.MessageBuilder;

export default guilded_webhook;
export {Webhook, MessageBuilder};
